import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-carplay-exemple-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
